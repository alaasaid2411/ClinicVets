# Tests

This folder is reserved for future test work.

Recommended test groups:

- Unit tests for validators and services.
- Functional tests for login, employee registration, and customer management.
- GUI tests for WinForms screens and user input behavior.

Good first candidates:

- `EmployeeValidator.ValidateUsername`
- `EmployeeValidator.ValidatePassword`
- `CustomerValidator.ValidatePhone`
- `AuthService.Login`
- `EmployeeService.RegisterEmployee`
- `CustomerService.RegisterCustomer`
